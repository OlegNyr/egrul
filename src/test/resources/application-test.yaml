logging:
  level:
    org.apache.http: INFO
    org.neo4j.ogm.drivers.bolt.request: DEBUG
    org.neo4j.ogm.drivers.embedded.request: DEBUG
spring:
  data:
    neo4j:
      embedded:
        enabled: true
      uri: file:///${java.io.tmpdir}/neo4j.db${random.long}
